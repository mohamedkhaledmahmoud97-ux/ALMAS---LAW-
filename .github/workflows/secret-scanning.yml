name: Secret Scanning Guidance

on:
  workflow_dispatch:

jobs:
  guidance:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder notice
        run: echo "Secret scanning is configured via GitHub repository settings."
